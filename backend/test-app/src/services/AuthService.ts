// TODO: Implement AuthService
// This service should handle:
// - User registration (hash password, create user, return tokens)
// - User login (verify credentials, return tokens)
// - Token refresh (validate refresh token, issue new pair)
// - Logout (invalidate refresh token)

// See README for the full interface specification
