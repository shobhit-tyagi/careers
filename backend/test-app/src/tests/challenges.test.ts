// TODO: Implement challenge integration tests
// Test cases to cover:
// - GET /api/challenges — returns paginated list
// - GET /api/challenges/:id — returns challenge detail
// - POST /api/challenges/:id/complete — points calculated correctly
// - POST /api/challenges/:id/complete — partial listen earns partial points
// - POST /api/challenges/:id/complete — 100% listen earns full points
// - Completing a challenge updates user's totalPoints
