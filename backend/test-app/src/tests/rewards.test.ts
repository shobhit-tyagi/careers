// TODO: Implement reward integration tests
// Test cases to cover:
// - GET /api/rewards — returns available rewards
// - POST /api/rewards/:id/redeem — success, points deducted
// - POST /api/rewards/:id/redeem — insufficient points returns error
// - GET /api/rewards/history — returns user's redemptions
// - Redemption is atomic (points deducted and redemption created together)
